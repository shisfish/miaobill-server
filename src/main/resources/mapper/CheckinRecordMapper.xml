<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miaobill.mapper.CheckinRecordMapper">
    <select id="findByUserIdAndDate" resultType="com.miaobill.entity.CheckinRecord">
        select * from checkin_record where user_id = #{userId} and checkin_date = #{checkinDate}
    </select>
    <select id="findByUserId" resultType="com.miaobill.entity.CheckinRecord">
        select * from checkin_record where user_id = #{userId} order by checkin_date desc
    </select>
    <select id="findLatestByUserId" resultType="com.miaobill.entity.CheckinRecord">
        select * from checkin_record where user_id = #{userId} order by checkin_date desc limit 1
    </select>
    <insert id="insert" parameterType="com.miaobill.entity.CheckinRecord" useGeneratedKeys="true" keyProperty="id">
        insert into checkin_record (user_id, checkin_date, continuous_days, create_time)
        values (#{userId}, #{checkinDate}, #{continuousDays}, #{createTime})
    </insert>
    <select id="countByUserId" resultType="int">
        select count(*) from checkin_record where user_id = #{userId}
    </select>
</mapper>
