spring:
  datasource:
    url: jdbc:mysql://localhost:3306/miaobill?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.miaobill.entity

server:
  port: 8080
